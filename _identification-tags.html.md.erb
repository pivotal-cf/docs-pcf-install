(Optional) Enter your comma-separated custom **Identification Tags.** For example, `iaas:foundation1, hello:world`. If you have multiple foundations, you can use the tags to identify each foundation when viewing VMs from your IaaS.<% if current_page.data.iaas == "Azure" %><p class="note"><strong>Note:</strong> Azure has limited space for identification tags. Pivotal recommends entering no more than five tags.</p><% end %>