In the **TLS Cipher Suites for HAProxy** field, specify the TLS cipher suites to use in HAProxy communications. Use an ordered, colon-delimited list of Golang-supported TLS cipher suites in the OpenSSL format. If you need to accommodate clients that use an older version of TLS, specify the cipher suites used by the older version of TLS. 
    <%= image_tag 'images/networking_tls_haproxy.png' %>
