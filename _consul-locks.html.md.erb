The **Advanced Features** section of Elastic Runtime includes a checkbox that disables using Consul for distributed locking in Diego.

Click **Skip Consul Locks** to disable using Consul for distributed locking in Diego. 

In a new installation, this checkbox is selected by default. In an upgrade, you must select this checkbox and redeploy. See [] section for more information.

  <%= image_tag('consul-locks.png') %>