To disable container-to-container networking, perform the following steps:
  1. Select **Disable**.
    <img alt="Specify the host port for receiving VXLAN packets" src="images/disablec2c.png">
  1. (Optional) Enter an IP range in the **Applications Subnet** box. If you do not
set a custom range, Ops Manager uses <code>10.254.0.0/22</code>.
