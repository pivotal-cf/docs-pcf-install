If you want to log traffic for application packets, choose **Enable** in **Log traffic for all accepted/denied application packets**. You can choose enter the denied logging interval in **Denied Logging Interval**. The default denied loggin interval is one packet per second. You can choose to enter the UDP logging interval in **UDP logging interval**. The default UDP logging interval is 100 packets per second. 
  <img alt="Traffic Application Packet" src="images/traffic-application-packet.png">