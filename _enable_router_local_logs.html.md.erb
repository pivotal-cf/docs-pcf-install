(Optional) To stop the Router from writing access logs to local disk, deselect the **Enable Router to write access logs locally** checkbox. For high traffic deployments, logs may not be rotated fast enough and can fill up the disk. Disable local logging by deslecting this checkbox.