(Optional) The **Enable Router to write access logs locally** checkbox is enabled by default. You should consider disabling this checkbox for high-traffic deployments, since logs may not be rotated fast enough and can fill up the disk. 