<% if $this_IaaS == 'AWS-cloudform'%>
1. If you do not know it, find the name of your ELB by clicking **Load Balancers** in the AWS EC2 dashboard.
<% end %>

1. In the **Elastic Runtime** tile, click **Resource Config**. In the **ELB Name** field of the **Router** row, enter the name of your load balancer. You may configure multiple load balancers by entering the names separated by commas.

1. Click **Save**.

    <%= image_tag("pcfaws/er-aws-resource-config.png") %>
