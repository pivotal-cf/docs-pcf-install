Navigate to the **File Storage Config** page of the Elastic Runtime product tile.

If you want to use PCF's internal file store, select **Internal** and click **Save**.

If you want to use an external S3-compatible filestore for your Elastic
Runtime file storage, complete the following procedure:

1. Select **External S3-Compatible Filestore**.

1. Enter the **URL Endpoint** for your filestore. If you manually configured Amazon Web Services for PCF, this is the value that you entered into the Director config page in [Configuring Ops Manager Director for AWS](./pcf-aws-om-config.html#pcfaws-om-dirconfig#pcfaws-om-dirconfig).

1. Enter the **Bucket Name**. If you manually configured Amazon Web Services for PCF, this is the name that you chose in [Create S3
Buckets](./pcf-aws-component-config.html#pcfaws-s3).

1. Enter your **Access Key** and **Secret Key**. If you are using Amazon Web Services, retrieve your key information with the IAM credentials that you generated in [Create an IAM User for PCF](./pcf-aws-component-config.html#pcfaws-iam-user).

1. Click **Save**.

    <%= image_tag("pcfaws/external_filestore_url_endpoint.png") %>
